describe '四則演算' do
    context '足し算' do
      it '1 + 1 は 2 になる' do
      end
      it '3 + 2 は 5 になる' do
      end
    end
    context '引き算' do
      it '3 - 1 は 2 になる' do
      end
      it '5 - 2 は 3 になる' do
      end
      
    end
end

context 'バリデーションによって正しいデータ以外保存しない' do
    
    before do
        '例：タイトルが入力されていないデータの作成'
    end
    it 'タイトルが入力されていない時は保存しない' do
    end
end